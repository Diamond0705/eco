services:
  db:
    image: postgres:16
    container_name: ecologist_db
    environment:
      POSTGRES_DB: ecologist
      POSTGRES_USER: ecologist
      POSTGRES_PASSWORD: ecologist
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
